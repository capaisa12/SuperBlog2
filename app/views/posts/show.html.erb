<div class="container">
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
          <h2><%= @post.title %></h2>
          <small>Publicado por <%= @post.email %> hace <%= @post.created_at %> </small>
          <h5><p><%= @post.content %></p></h5>
    </div>
  </div>
</div>
